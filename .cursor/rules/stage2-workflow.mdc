---
description: Конвенции workflow classification-stage2-dev (имена, контракт, layout)
globs: workflows/classification-stage2-dev.json,scripts/**stage2**,scripts/phase3_nodes/**,Categories/stage2_workflow_contract.md
alwaysApply: false
---

# Stage 2 workflow conventions

Перед правками прочитай `Categories/stage2_workflow_contract.md`.

## Имена нод

- Префикс зоны: `In —`, `Run —`, `Load —`, `P1 —`, `2A —`, `2B —`, `Judge —`, `DB —`, `Fin —`, `Shared —`
- Запрещены generic-имена: `Code`, `Merge`, `AI Agent`, `Post-process` без префикса
- Ссылка на run: `$('Run — Create Run')`, не `$('Create Run')`

## Code-ноды

- Всегда `...item.json` и `pairedItem`
- Константы из `item.json.constants` (Init Constants), с локальным fallback
- Не использовать `$('…')` на ноды из параллельных веток — только через Merge Context

## Layout (swimlanes)

- Субпроцессы **сверху вниз**: Setup → P1 → 2A → 2B → Judge → DB/Fin
- Внутри полосы — слева направо; Route в конце полосы, выход в DB — вниз
- Шаг колонок ~220px; Chat Model — **под** AI Agent своей зоны (`P1 — DeepSeek`, `2A — DeepSeek`, `2B — DeepSeek`; Judge: `Shared — Polza`)
- Sticky note + цвет preset для каждого субпроцесса (см. контракт §7)
- После правок позиций: `python3 scripts/reorganize_stage2_layout.py`

## Деплой

```bash
python3 scripts/pull_workflow.py classification-stage2-dev
# правки
python3 scripts/push_workflow.py classification-stage2-dev
```

Не менять `classification-stage2-prepare-for-llm`.
